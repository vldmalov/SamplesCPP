#include <iostream>
//#include <vector>

int main()
{
	//std::vector<int> userData = { 1, 2, 4, 9, 16, 25, 36 };

	std::cin.ignore();
	std::cin.get();

	return 0;
}

// 2. Размер вектора 
// std::cout << "userData size = " << userData.size() << std::endl;

// 3. Способы итерирования по вектору (for, range-based for)
/*
for (int idx = 0; idx < userData.size(); ++idx)
{
	std::cout << "Item " << idx << " = " << userData[idx] << std::endl;
}
*/

// 4. Доступ черех квадратные скобки
//userData[3] = 12;

// 5. Добавление в конец
/*
userData.push_back(49);
userData.push_back(64);
userData.push_back(81);
userData.push_back(10);
*/
